%h1 Listing orders

%table
  %tr
    %th Orderid
    %th Order Placed

  - @orders.each do |order|
    %tr
      %td= order.id
      %td= order.order_placed
      %td= link_to 'Show', order
%br

=link_to "New Order", new_order_path
